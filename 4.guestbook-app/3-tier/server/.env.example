# 3-Tier · 데이터 계층 접속 정보
#
# 1-Tier에서는 DB_HOST가 localhost 였다 — DB가 같은 EC2 안에 있었으니까.
# 쪼개고 나면 DB는 **RDS**에 있다. 그래서 주소를 알아야 한다.
#
#   DB_HOST=<RDS 엔드포인트>   예) guestbook-db.abc123.us-east-1.rds.amazonaws.com
#
# 이 파일은 서버(EC2)에만 둔다. 화면(S3)으로는 절대 나가지 않는다.
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=
