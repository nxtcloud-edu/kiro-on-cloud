# 화면이 서버(FastAPI)를 찾아가는 주소.
# 화면은 S3에, 서버는 EC2에 있으므로 서로의 위치를 알려줘야 한다.
#
#   예) SERVER_URL=http://12.34.56.78:8000
#
# 이 값은 `npm run build` 시점에 정적 파일 안으로 박힌다.
# → IP가 바뀌면 .env만 고치지 말고 반드시 다시 빌드해서 업로드할 것.
SERVER_URL=
